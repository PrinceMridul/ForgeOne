{
  "name": "@forgeone/database",
  "version": "0.1.0",
  "private": true,
  "description": "Prisma database schema and client",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc", "clean": "rm -rf dist", "type-check": "tsc --noEmit",
    "db:generate": "prisma generate", "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push", "db:seed": "prisma db seed", "db:studio": "prisma studio"
  },
  "dependencies": { "@prisma/client": "^6.0.0" },
  "devDependencies": {
    "@forgeone/config": "workspace:*",
    "@types/node": "^22.0.0",
    "prisma": "^6.0.0",
    "typescript": "^5.7.0"
  },
  "prisma": { "schema": "prisma/schema.prisma" }
}
