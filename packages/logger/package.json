{
  "name": "@forgeone/logger",
  "version": "0.1.0",
  "private": true,
  "description": "Shared structured logging",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": { "build": "tsc", "clean": "rm -rf dist", "type-check": "tsc --noEmit" },
  "dependencies": { "pino": "^9.0.0", "pino-pretty": "^11.0.0" },
  "devDependencies": {
    "@forgeone/config": "workspace:*",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
